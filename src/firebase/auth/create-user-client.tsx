"use client";

import { useEffect } from 'react';
import { createAdminUser } from './create-user';

// This component is currently not used to prevent errors.
// Enable it once Email/Password provider is configured in Firebase.
export default function CreateAdminUser() {
  // useEffect(() => {
  //   createAdminUser();
  // }, []);

  return null;
}
